# Wave

Пет-проект: аналіз музики через перетворення Фур'є, автоматичний пошук
схожих треків, безшовне міксування і синхронізоване керування світлом
для домашніх міні-концертів.

## Три ключові фічі

1. **Аналіз** — розкласти трек по STFT на частотні характеристики і
   витягнути набір ознак (тембр, гармонія, ритм, енергія, структура).
2. **Схожість і мікс** — знайти в базі найближчі треки і склеїти їх
   у безперервний сет. Спочатку — beat-aligned crossfade, потім —
   розумніші переходи.
3. **Світло** — API, яке віддає світлову доріжку, синхронізовану
   з міксом, у реальне залізо (LED-стрічка, DMX) або в емулятор.

## Цільовий матеріал

Основне — **електроніка**: постійний темп, чіткі 8/16/32-тактові фрази,
чисті intro/outro. Найзручніший випадок, під нього написано все.

**Рок і живі записи** — пізніше. Вони ламають три припущення (плаваючий
темп, нерівні фрази, ненадійна хрома на дисторшні). Правила, які треба
дотриматись зараз, щоб потім не переписувати —
у [03](docs/03-similarity-and-mixing.md#жанрові-відмінності).

## Стек

| Шар | Технологія | Чому |
|---|---|---|
| Ядро аналізу/міксу | Python 3.11+ (librosa, numpy, soundfile, pyloudnorm, pyrubberband) | Готові реалізації beat tracking, chroma, MFCC. Немає сенсу писати це з нуля |
| API / оркестрація | ASP.NET Core 8 | Робочий стек, SignalR для реалтайму, швидкий UDP-вивід для світла |
| Світловий движок | .NET (Wave.Lighting) | Стабільний таймінг, UDP 40–60 FPS |
| Web UI | Angular | Знайомий стек, візуалізація спектру в браузері |
| БД | PostgreSQL + pgvector | Реляційні метадані + ANN-пошук по векторах тембру в одній базі |
| Кеш аналізу | .npz / Parquet файли на диску | Покадрові дані завеликі для рядків БД |

## Документи

| Файл | Про що |
|---|---|
| [docs/01-architecture.md](docs/01-architecture.md) | Компоненти, потоки даних, схема БД, лейаут репозиторію |
| [docs/02-audio-analysis.md](docs/02-audio-analysis.md) | Що саме рахуємо з Фур'є і як зберігаємо |
| [docs/03-similarity-and-mixing.md](docs/03-similarity-and-mixing.md) | Пошук схожих треків і алгоритм переходів |
| [docs/04-lighting.md](docs/04-lighting.md) | API і драйвери освітлення, вибір заліза |
| [docs/05-roadmap.md](docs/05-roadmap.md) | Мілстоуни M0–M7 |
| [docs/tasks/](docs/tasks/) | Деталізовані завдання по мілстоунах |

## Робочі нотатки

Документація і код — тут, у репо. Це єдине джерело правди.

Спостереження, здогадки й те, чому в git не місце, живуть в Obsidian
(`Documents/Obsidian/yuron/Pet проекти/Wave/`):

| Нотатка | Про що |
|---|---|
| `Wave` | Хаб: прогрес по мілстоунах, прийняті рішення, відкриті питання |
| `Wave — Журнал аналізу` | Треки, де аналіз промахнувся; треки-еталони |
| `Wave — Журнал схожості` | Промахи підбору, історія зміни ваг скорингу |
| `Wave — Залізо` | Що куплено, IP стрічки, налаштування WLED, калібрування затримки |

## Головний принцип

**Усе, що можна порахувати заздалегідь — рахуємо заздалегідь.**

Мікс рендериться у файл, світлова доріжка рендериться у масив кадрів.
Під час "концерту" програма лише програє готове аудіо і шле готові кадри
по одному годиннику. Це знімає найважчу проблему проекту — реалтайм-DSP
із жорстким таймінгом — і залишає її на потім, коли (і якщо) захочеш
живий режим.

---

## Швидкий старт (M0 — Каркас)

### 1. Передумови (Windows)
- **Python 3.11+**
- **.NET 9 SDK**
- **ffmpeg** (потрібен для `librosa` при роботі з MP3):
  ```powershell
  winget install Gyan.FFmpeg
  ```
- **rubberband-cli** (для тайм-стретчингу в M3):
  ```powershell
  winget install BreakfastQuay.RubberBand
  ```

### 2. База даних (PostgreSQL)
Підключення здійснюється напряму до локальної служби PostgreSQL (без Docker):
- Переконайтеся, що на вашому сервері PostgreSQL створено базу даних (наприклад, `wave`) та встановлено розширення `vector` (`CREATE EXTENSION IF NOT EXISTS vector;`).
- Створіть файл `src/wave-core/.env` (за зразком `.env.example`) та вкажіть ваш connection string:
  ```env
  WAVE_DB=postgresql://postgres:your_password@localhost:5432/wave
  ```
- Для `Wave.Api` вкажіть рядок підключення у `src/Wave.Api/appsettings.Development.json`:
  ```json
  "ConnectionStrings": {
    "WaveDb": "Host=localhost;Port=5432;Database=wave;Username=postgres;Password=your_password"
  }
  ```
*(За бажанням `docker-compose.yml` залишається доступним як альтернативний варіант для запуску в ізольованому контейнері).*

### 3. Налаштування wave-core (Python)
Перейдіть у папку ядра та налаштуйте віртуальне середовище:
```powershell
cd src/wave-core
python -m venv .venv
.\.venv\Scripts\activate
pip install -e ".[dev]"

# Запуск міграцій схеми БД
wave migrate

# Перевірка підключення та таблиць
wave db-check

# Запуск API сервера wave-core (порт 8100)
uvicorn wave_core.api:app --port 8100 --reload
```

### 4. Запуск Wave.Api (.NET)
В окремому терміналі з кореня проекту:
```powershell
dotnet run --project src/Wave.Api --urls http://localhost:5020
```

### 5. Перевірка працездатності (Health Check)
Зверніться до ендпоінту Wave.Api, який опитує wave-core та повертає зведений стан:
```powershell
curl http://localhost:5020/health
# Відповідь: {"api":"ok","core":"ok","core_db":"ok","timestamp":"..."}
```

